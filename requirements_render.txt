# Minimal requirements for Render deployment
# Only essential packages for render_app_simple.py and render_app.py

# Web Framework (Required)
fastapi==0.104.1
uvicorn[standard]==0.24.0
starlette==0.27.0

# Core dependencies
python-dateutil==2.8.2
anyio>=3.7.1,<4.0.0
click==8.1.7
pydantic==2.5.0

# System utilities
psutil==5.9.6
requests==2.31.0

# Data Processing (required for sports_analytics_agent)
# Note: Using Python 3.11 to avoid compilation issues
pandas==2.1.3
numpy==1.26.2
scipy==1.11.4

# Machine Learning (required for sports_analytics_agent)
scikit-learn==1.3.2
xgboost==2.0.3
lightgbm==4.1.0

# Optional: TensorFlow (for sequence models - agent handles gracefully if missing)
# tensorflow-cpu==2.15.0  # Uncomment if needed (very heavy, may not work on free tier)
